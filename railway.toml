[build]
builder = "nixpacks"
buildCommand = "cd fitrack && rm -rf dist && npm ci && npm run build"

[deploy]
startCommand = "npm start"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyMaxRetries = 10

[envs]
NODE_ENV = "production"
NIXPACKS_NODE_VERSION = "18"
